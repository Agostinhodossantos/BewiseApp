package com.app.bewise.model;

import android.graphics.drawable.Drawable;

public class Subject extends BookCategory {
    public Subject(String id, String title, int img) {
        super(id, title, img);
    }
}
